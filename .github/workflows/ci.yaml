# Copyright (c) 2022 MobileCoin Inc.
name: ci

on:
  pull_request: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  pull-requests: write
  contents: read

jobs:
  # Build docker container to test, but don't push
  # docker:
  #   runs-on: [self-hosted, Linux, small]
  #   steps:
  #   - name: Checkout
  #     uses: actions/checkout@v3

  #   - name: Docker meta
  #     id: meta
  #     uses: docker/metadata-action@v4
  #     with:
  #       flavor: latest=true
  #       images: ${{ env.DOCKER_REPO }}
  #       tags: |
  #         type=ref,event=branch
  #         type=semver,pattern=v{{version}}
  #         type=sha

  #   - name: Set up Docker Buildx
  #     uses: docker/setup-buildx-action@v2

  #   - name: Login to DockerHub
  #     uses: docker/login-action@v2
  #     with:
  #       username: ${{ secrets.DOCKERHUB_USERNAME }}
  #       password: ${{ secrets.DOCKERHUB_TOKEN }}

  #   - name: Build and maybe push container
  #     uses: docker/build-push-action@v3
  #     with:
  #       context: .
  #       file: .internal-ci/docker/Dockerfile.infra-haproxy-blocklist
  #       labels: ${{ steps.meta.outputs.labels }}
  #       tags: ${{ steps.meta.outputs.tags }}
  #       push: false

  # Run each check as a separate action. This way we can get all the errors in one go.
  # lint-python:
  #   runs-on: [self-hosted, Linux, small]
  #   steps:
  #   - name: Checkout
  #     uses: actions/checkout@v3

  #   - name: Set up Python environment
  #     uses: actions/setup-python@v4
  #     with:
  #       python-version: "3.10"

  #   - name: Run flake8 with reviewdog
  #     uses: reviewdog/action-flake8@v3
  #     with:
  #       fail_on_error: true
  #       reporter: github-pr-review

  lint-docker:
    runs-on: [self-hosted, Linux, small]
    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Run hadolint with reviewdog
      uses: reviewdog/action-hadolint@v1
      with:
        fail_on_error: true
        reporter: github-pr-review
        exclude: |
          *.dockerignore

  lint-shell:
    runs-on: [self-hosted, Linux, small]
    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Run shellcheck with reviewdog
      uses: reviewdog/action-shellcheck@v1
      with:
        fail_on_error: true
        reporter: github-pr-review

  lint-actions:
    runs-on: [self-hosted, Linux, small]
    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Run actionlint with reviewdog
      uses: reviewdog/action-actionlint@v1
      with:
        fail_on_error: true
        reporter: github-pr-review

  # no reviewdog for helm
  # lint-helm:
  #   runs-on: [self-hosted, Linux, small]
  #   container:
  #     image: mobilecoin/gha-k8s-toolbox:v1
  #   steps:
  #   - name: Checkout
  #     uses: actions/checkout@v3

  #   - name: Run helm lint
  #     run: |
  #       chart_base=".internal-ci/helm"
  #       chart_files=$(find "${chart_base}" -name Chart.yaml -type f)

  #       # helm lint on directories where there are chart.yaml files
  #       for c in ${chart_files}
  #       do
  #         chart=$(dirname "${c}")
  #         helm lint --quiet "${chart}"  2>&1
  #       done

  # Have one noop job at the end that passes if all the other checks pass.
  # This is so we can just have one "standard" check to add to the branch protections.
  workflow-ok:
    needs:
    # - docker
    # - lint-python
    - lint-docker
    - lint-shell
    - lint-actions
    # - lint-helm
    runs-on: [self-hosted, Linux, small]
    steps:
    - name: All Checks OK!
      run: |
        true
